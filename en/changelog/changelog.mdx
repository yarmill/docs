---
title: "Changelog"
description: "Product updates and announcements from Yarmill."
mode: "wide"
---

{/* TODO(yarmill): replace the example entry below with real release notes.
   Add the newest entry at the top. Each <Update> takes a `label` (the date or
   period) and an optional `description` (e.g. a version tag). */}

<Update label="June 2026">
  First release of the new Yarmill documentation — guides for coaches, athletes,
  and administrators across every module.
</Update>
