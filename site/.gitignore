node_modules
.next
.source
out
next-env.d.ts
.netlify
*.tsbuildinfo
.DS_Store

# Generated by scripts/build-search-index.mjs (prebuild) — fetched lazily by the
# search palette as a static asset.
public/search-index.json

# Local env (Linear API key etc.) — never commit
.env
.env.local
.env*.local

# Linear sync raw cache + intermediate curation artifacts
scripts/.cache
